---
# Deployment for MySQL
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-deployment
  labels:
    environment: mysql
    tier: database
spec:
  selector:
    matchLabels:
      environment: mysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        environment: mysql
        tier: database
    spec:
      containers:
      - name: mysql
        image: mysql:latest
        env:
        - name: MYSQL_ALLOW_EMPTY_PASSWORD
          value: "true"
        - name: MYSQL_DATABASE
          value: mysql # valor retirado da imagem do ghost da base de dados original que foi apagado
        - name: MYSQL_USER
          value: mysql # valor retirado da imagem do ghost da base de dados original que foi apagado
        - name: MYSQL_PASSWORD
          value: #empty assim ou assim: "" ????????????
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pv-claim
